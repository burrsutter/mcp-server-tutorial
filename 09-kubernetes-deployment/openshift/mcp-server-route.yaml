apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mcp-route
  labels:
    app: mcp-server
spec:
  to:
    kind: Service
    name: mcp-server-service
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
