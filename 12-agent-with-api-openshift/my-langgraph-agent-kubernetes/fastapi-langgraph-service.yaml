apiVersion: v1
kind: Service
metadata:
  name: my-langgraph-agent-service
  labels:
    app: my-langgraph-agent
spec:
  type: ClusterIP
  selector:
    app: my-langgraph-agent
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
